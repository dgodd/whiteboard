<h2>New item</h2>

<%= render "form.html", changeset: @changeset,
                        kinds: @kinds,
                        action: item_path(@conn, :create) %>

<%= link "Back", to: item_path(@conn, :index) %>
